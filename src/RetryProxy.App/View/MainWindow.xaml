<ui:FluentWindow x:Class="RetryProxy.View.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                 xmlns:behavior="clr-namespace:RetryProxy.View.Behavior"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:markup="clr-namespace:RetryProxy.Markup"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:pages="clr-namespace:RetryProxy.View.Pages"
                 xmlns:tray="http://schemas.lepo.co/wpfui/2022/xaml/tray"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:viewModel="clr-namespace:RetryProxy.ViewModel"
                 xmlns:i18n="clr-namespace:RetryProxy.Service.I18n"
                 Title="{i18n:T LLM Retry Proxy}"
                 Width="900"
                 Height="600"
                 MinWidth="720"
                 MinHeight="480"
                 d:Background="#D2D2D2"
                 d:DataContext="{d:DesignInstance Type=viewModel:MainWindowViewModel}"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="{StaticResource TextThemeFontFamily}"
                 FontSize="12"
                 Visibility="{Binding IsVisible, Mode=TwoWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                 WindowBackdropType="Auto"
                 WindowStartupLocation="CenterScreen"
                 WindowState="{Binding WindowState, Mode=TwoWay}"
                 mc:Ignorable="d">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Activated">
            <b:InvokeCommandAction Command="{Binding ActivatedCommand}" />
        </b:EventTrigger>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </b:EventTrigger>
        <b:EventTrigger EventName="Closing">
            <b:InvokeCommandAction Command="{Binding ClosingCommand}" PassEventArgsToCommand="True" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:NavigationView x:Name="RootNavigation"
                           Grid.Row="1"
                           IsBackButtonVisible="Collapsed"
                           IsPaneToggleVisible="True"
                           IsTopSeparatorVisible="False"
                           IsFooterSeparatorVisible="False"
                           OpenPaneLength="160">
            <b:Interaction.Behaviors>
                <behavior:AnimatedNavigationSelectionIndicatorBehavior />
            </b:Interaction.Behaviors>
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="{i18n:T 首页}"
                                       NavigationCacheMode="Enabled"
                                       TargetPageType="{x:Type pages:HomePage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Home24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

                <ui:NavigationViewItem Content="{i18n:T 运行状态}"
                                       NavigationCacheMode="Enabled"
                                       TargetPageType="{x:Type pages:StatusPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Pulse24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

                <ui:NavigationViewItem Content="{i18n:T 运行日志}"
                                       NavigationCacheMode="Enabled"
                                       TargetPageType="{x:Type pages:LogPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="DocumentText24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

                <ui:NavigationViewItem Content="{i18n:T 缓存明细}"
                                       NavigationCacheMode="Enabled"
                                       TargetPageType="{x:Type pages:CachePage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Database24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.MenuItems>
            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="{i18n:T 设置}"
                                       NavigationCacheMode="Enabled"
                                       TargetPageType="{x:Type pages:SettingsPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Settings24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="{i18n:T 关于}"
                                       NavigationCacheMode="Enabled"
                                       TargetPageType="{x:Type pages:AboutPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Info24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.FooterMenuItems>

            <ui:NavigationView.ContentOverlay>
                <Grid>
                    <ui:SnackbarPresenter x:Name="SnackbarPresenter" />
                    <ContentPresenter x:Name="RootContentDialogPresenter" />
                </Grid>
            </ui:NavigationView.ContentOverlay>
        </ui:NavigationView>

        <ui:TitleBar Grid.Row="0">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Resources/Images/logo.png" />
            </ui:TitleBar.Icon>
            <ui:TitleBar.Header>
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <b:Interaction.Behaviors>
                        <behavior:WindowDragBehavior />
                    </b:Interaction.Behaviors>
                    <TextBlock VerticalAlignment="Center" Text="{Binding Title, Mode=OneWay}" />
                </StackPanel>
            </ui:TitleBar.Header>
            <ui:TitleBar.TrailingContent>
                <StackPanel VerticalAlignment="Top" Orientation="Horizontal">
                    <ui:Button Width="40"
                               Height="32"
                               Background="Transparent"
                               BorderBrush="Transparent"
                               Command="{Binding SwitchBackdropCommand}"
                               CornerRadius="0"
                               Icon="{ui:SymbolIcon Blur24}"
                               ToolTip="{i18n:T 切换深浅主题或背景样式}" />
                    <ui:Button Width="32"
                               Height="32"
                               Background="Transparent"
                               BorderBrush="Transparent"
                               AutomationProperties.Name="{i18n:T 最小化到托盘}"
                               Command="{Binding HideCommand}"
                               CornerRadius="0"
                               Icon="{ui:SymbolIcon CaretDown24}"
                               ToolTip="{i18n:T 最小化到托盘}" />
                </StackPanel>
            </ui:TitleBar.TrailingContent>
        </ui:TitleBar>

        <tray:NotifyIcon Grid.Row="0"
                         FocusOnLeftClick="False"
                         Icon="pack://application:,,,/Resources/Images/logo.ico"
                         LeftDoubleClick="OnNotifyIconLeftDoubleClick"
                         MenuOnRightClick="True"
                         TooltipText="LLM Retry Proxy">
            <tray:NotifyIcon.Menu>
                <ContextMenu>
                    <ContextMenu.DataContext>
                        <markup:ServiceLocator Type="{x:Type viewModel:NotifyIconViewModel}" />
                    </ContextMenu.DataContext>
                    <ui:MenuItem Command="{Binding ShowOrHideCommand}"
                                 Header="{i18n:T 显示窗口}"
                                 Icon="{ui:SymbolIcon Window24}" />
                    <Separator />
                    <ui:MenuItem Command="{Binding EnableAllCommand}"
                                 Header="{i18n:T 全部启用}"
                                 Icon="{ui:SymbolIcon Play24}" />
                    <ui:MenuItem Command="{Binding DisableAllCommand}"
                                 Header="{i18n:T 全部停用}"
                                 Icon="{ui:SymbolIcon Stop24}" />
                    <Separator />
                    <ui:MenuItem Command="{Binding ExitCommand}"
                                 Header="{i18n:T 退出}"
                                 Icon="{ui:SymbolIcon Dismiss24}" />
                </ContextMenu>
            </tray:NotifyIcon.Menu>
        </tray:NotifyIcon>
    </Grid>
</ui:FluentWindow>

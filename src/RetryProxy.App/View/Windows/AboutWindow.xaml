<ui:FluentWindow x:Class="RetryProxy.View.Windows.AboutWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:i18n="clr-namespace:RetryProxy.Service.I18n"
                 Title="{i18n:T 关于}"
                 Width="420"
                 Height="280"
                 Background="#202020"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="{DynamicResource TextThemeFontFamily}"
                 WindowBackdropType="Auto"
                 WindowStartupLocation="CenterOwner"
                 mc:Ignorable="d">
    <Grid>
        <ui:Grid Margin="0,48,0,0" RowDefinitions="Auto,*,Auto">
            <ui:TextBlock Grid.Row="0" Margin="16,0,16,0" FontSize="16" FontWeight="Bold" Text="{i18n:T LLM Retry Proxy}" />
            <ui:TextBlock Grid.Row="1" Margin="16,0,16,0" TextWrapping="Wrap">
                <Run Text="{i18n:T 版本: }" />
                <Run x:Name="VersionRun" />
                <LineBreak />
                <Run Text="{i18n:T 协议: GPLv3}" />
                <LineBreak />
                <Run Text="{i18n:T 界面框架基于 }" />
                <Hyperlink NavigateUri="https://github.com/babalae/better-genshin-impact"
                           RequestNavigate="Hyperlink_RequestNavigate">
                    <Run Text="BetterGI" />
                </Hyperlink>
            </ui:TextBlock>
            <ui:Button Grid.Row="2" Margin="16" HorizontalAlignment="Right" Width="80" Content="{i18n:T 关闭}"
                       Click="CloseButton_Click" />
        </ui:Grid>
        <ui:TitleBar Title="{i18n:T 关于}">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Resources/Images/logo.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
    </Grid>
</ui:FluentWindow>
